clc, clear all;

disp("===================================");
disp("RSA cypher script for Octave/MATLAB");
disp("===================================");

%p = GetPrimeNumber(512);
disp(sprintf('%X', GetPrimeNumber(512)));

